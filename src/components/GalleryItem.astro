---
interface Props {
  src: string;
  alt: string;
  caption: string;
}

const { src, alt, caption } = Astro.props;
---

<figure class="flex flex-col">
  <div class="aspect-square overflow-hidden rounded-sm bg-brown/10">
    <img
      src={src}
      alt={alt}
      class="w-full h-full object-cover"
      loading="lazy"
    />
  </div>
  <figcaption class="font-body mt-2 text-sm text-brown italic text-center">
    {caption}
  </figcaption>
</figure>
